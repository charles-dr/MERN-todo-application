import { createUseStyles } from 'react-jss';

const useDefaultLayoutStyles = createUseStyles({
  container: {
    padding: '10% 1rem',
  },
  title: {
    margin: '1rem 0',
  },
});

export default useDefaultLayoutStyles;
